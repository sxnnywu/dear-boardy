# ── NEVER commit feedback data (contains ~600 real users' messages / PII) ──
raw/
data/
digests/

# OS / editor / Python junk
.DS_Store
__pycache__/
*.pyc
.venv/
venv/
.env

# pytest
.pytest_cache/

# scheduled-run logs (may echo feedback content)
logs/

# eval predictions are regenerated outputs, not source (golden_set.jsonl is the truth)
evals/predictions*.jsonl
